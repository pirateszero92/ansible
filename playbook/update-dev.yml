---
- hosts: all

  become: true
  tasks:
    - name: Update APT package index
      apt:
        update_cache: yes

    - name: Upgrade all packages to the latest version
      apt:
        upgrade: dist
